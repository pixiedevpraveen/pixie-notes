{
  "private": true,
  "name": "pixie",
  "type": "module",
  "version": "0.1.5",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 3005 --dotenv .env.local",
    "start": "nuxt dev --port 3005 --dotenv .env",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "sync": "cap sync",
    "ionic:build": "npm run generate && npx cap copy",
    "ionic:serve": "npm run dev"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.3.0",
    "nuxt": "^3.5.1",
    "nuxt-vite": "^0.3.5"
  },
  "dependencies": {
    "@capacitor/android": "^5.3.0",
    "@capacitor/app": "^5.0.6",
    "@capacitor/core": "^5.3.0",
    "@capacitor/local-notifications": "^5.0.6",
    "@capacitor/preferences": "^5.0.6",
    "@capacitor/share": "^5.0.6",
    "indexeddb-orm": "^4.0.0-alpha.3",
    "magic-regexp": "^0.7.0",
    "pocketbase": "^0.18.0"
  }
}